# Spring配置
spring:
  # 数据源设置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/yurayura_new?serverTimezone=GMT%2B8&useSSL=false&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      auto-commit: true
      idle-timeout: 180000
      minimum-idle: 15
      maximum-pool-size: 100
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
      pool-name: ELiaNaCcHikariCP
  # 发送邮箱配置
  mail:
    host: smtp.126.com
    username: elianacc@126.com
    password: cny123456
    default-encoding: UTF-8

# 自定义配置
yurayura:
  # 番剧上传默认图片
  default-upload:
    image: images/tpjxz.jpg
  # swagger是否启用
  swagger:
    enable: true
  # 接收邮箱
  receive-email: elianacc@126.com
  # 上传资源对外暴露的访问路径(虚拟路径)和真实路径
  file:
    virtual-path: /upload/**
    upload-path: D://yurayura_v11/upload/